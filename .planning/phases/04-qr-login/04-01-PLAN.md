---
phase: 04-qr-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/app/common/api.py
  - tests/test_pan_api.py
autonomous: true
requirements: [QRC-02, QRC-03]
user_setup: []

must_haves:
  truths:
    - "Pan123 类提供 qr_generate() 方法，调用 login.123pan.com/api/user/qr-code/generate 返回 uniID 和 url"
    - "Pan123 类提供 qr_poll() 方法，调用 login.123pan.com/api/user/qr-code/result 返回 loginStatus 和 token"
    - "qrcode 和 Pillow 已加入 pyproject.toml 依赖"
  artifacts:
    - path: "pyproject.toml"
      provides: "qrcode + Pillow 依赖声明"
      contains: "qrcode"
    - path: "src/app/common/api.py"
      provides: "qr_generate 和 qr_poll API 方法"
      exports: ["qr_generate", "qr_poll"]
    - path: "tests/test_pan_api.py"
      provides: "QR API 方法的单元测试"
      contains: "test_qr_generate"
  key_links:
    - from: "src/app/common/api.py"
      to: "login.123pan.com"
      via: "requests session GET"
      pattern: "login\\.123pan\\.com/api/user/qr-code"
---

<objective>
为 Pan123 类新增二维码登录 API 方法，并添加依赖库。

Purpose: 提供 QR 登录的 API 基础层，供 UI 层调用。这是二维码登录功能的数据通道。
Output: Pan123.qr_generate() 和 Pan123.qr_poll() 方法 + pyproject.toml 新依赖 + 单元测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-qr-login/04-CONTEXT.md

<interfaces>
<!-- Pan123 类现有模式（executor 需要遵循的代码风格） -->

From src/app/common/api.py:
```python
class Pan123:
    def __init__(self, readfile=True, user_name="", password="", authorization="", input_pwd=False):
        # ...
        self.session = requests.Session()
        self._session_lock = threading.Lock()
        self.header_logined = { ... }  # 包含 loginuuid, platform, devicetype 等

    def _api_request(self, method, url, max_token_refreshes=1, **kwargs):
        # 统一请求封装，带 429 检测和 token 刷新

    def user_info(self):
        res = self._api_request(self.session.get, "https://api.123pan.cn/b/api/user/info", ...)
        res_json = _parse_json_response(res)
        # code==0 返回 data，否则 None
```

From doc/api_web.md Section 8:
```
GET https://login.123pan.com/api/user/qr-code/generate
Response: { code: 0, data: { uniID: "...", url: "https://..." } }

GET https://login.123pan.com/api/user/qr-code/result?uniID={uniID}
Response: { code: 0, data: { loginStatus: 0|1|2, token?: "eyJ..." } }

Headers required: loginuuid, app-version: 3, platform: web
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pan123 新增 qr_generate() 和 qr_poll() + 依赖 + 测试</name>
  <files>pyproject.toml, src/app/common/api.py, tests/test_pan_api.py</files>
  <read_first>
    - src/app/common/api.py (当前完整源码，理解 Pan123 类结构、_api_request 封装、header_logined 格式)
    - doc/api_web.md (第 8 节扫码登录 API 文档，获取 URL、请求参数、响应格式)
    - pyproject.toml (当前依赖列表，确认添加位置)
    - tests/test_pan_api.py (现有测试模式，复用 mock 风格)
  </read_first>
  <behavior>
    - Test 1: qr_generate 成功时返回 dict 包含 uniID 和 url 两个 key
    - Test 2: qr_generate 网络异常时抛出 requests.RequestException
    - Test 3: qr_generate API 返回 code!=0 时抛出 RuntimeError
    - Test 4: qr_poll 返回 loginStatus=0 时返回 {"loginStatus": 0}
    - Test 5: qr_poll 返回 loginStatus=2 时返回包含 token 的 dict
    - Test 6: qr_poll 网络异常时抛出 requests.RequestException
  </behavior>
  <action>
1. pyproject.toml: 在 dependencies 列表中追加 "qrcode>=8.0" 和 "Pillow>=11.0.0"（在 zstandard 之后）

2. src/app/common/api.py Pan123 类中新增两个方法：

```python
def qr_generate(self):
    """获取二维码登录会话（uniID + url）。"""
    headers = {
        "loginuuid": self.loginuuid,
        "app-version": "3",
        "platform": "web",
        "content-type": "application/json;charset=UTF-8",
    }
    with self._session_lock:
        res = self.session.get(
            "https://login.123pan.com/api/user/qr-code/generate",
            headers=headers,
            timeout=10,
        )
    res_json = _parse_json_response(res)
    code = res_json.get("code", -1)
    if code != 0:
        raise RuntimeError(
            f"获取二维码失败: code={code}, "
            f"message={res_json.get('message', '')}"
        )
    data = res_json.get("data", {})
    return {"uniID": data.get("uniID", ""), "url": data.get("url", "")}


def qr_poll(self, uni_id):
    """轮询二维码扫码状态。返回 dict 包含 loginStatus 和可能的 token。"""
    headers = {
        "loginuuid": self.loginuuid,
        "app-version": "3",
        "platform": "web",
        "content-type": "application/json;charset=UTF-8",
    }
    with self._session_lock:
        res = self.session.get(
            "https://login.123pan.com/api/user/qr-code/result",
            headers=headers,
            params={"uniID": uni_id},
            timeout=10,
        )
    res_json = _parse_json_response(res)
    code = res_json.get("code", -1)
    if code != 0:
        raise RuntimeError(
            f"轮询扫码状态失败: code={code}, "
            f"message={res_json.get('message', '')}"
        )
    data = res_json.get("data", {})
    result = {"loginStatus": data.get("loginStatus", -1)}
    token = data.get("token")
    if token:
        result["token"] = token
    return result
```

注意：
- qr_generate/qr_poll 使用独立 headers（platform=web），不走 _api_request（因为 QR API 域名不同，不需要 token 刷新）
- 使用 self._session_lock 保护 session 调用
- 不用 _api_request 的原因：QR API 在 login.123pan.com 域名，不需要 authorization header，不需要 token 刷新机制

3. tests/test_pan_api.py 中新增 TestQrGenerate 和 TestQrPoll 测试类，使用 unittest.mock.patch 模拟 session.get 响应
  </action>
  <verify>
    <automated>cd /Users/easyops/coding/123pan && python -m pytest tests/test_pan_api.py -x -v -k "qr" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml 包含 "qrcode>=8.0" 和 "Pillow>=11.0.0" 在 dependencies 列表中
    - src/app/common/api.py 包含 `def qr_generate(self):`
    - src/app/common/api.py 包含 `def qr_poll(self, uni_id):`
    - src/app/common/api.py 中 qr_generate 请求 URL 为 "https://login.123pan.com/api/user/qr-code/generate"
    - src/app/common/api.py 中 qr_poll 请求 URL 为 "https://login.123pan.com/api/user/qr-code/result"
    - tests/test_pan_api.py 包含 class TestQrGenerate 和 class TestQrPoll
    - pytest tests/test_pan_api.py -k "qr" 全部通过（至少 6 个测试）
  </acceptance_criteria>
  <done>Pan123 类具备 qr_generate() 和 qr_poll() 方法，pyproject.toml 包含 qrcode+Pillow 依赖，6+ 个 QR API 测试全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> login.123pan.com | QR generate/poll 请求跨越到外部 API |
| poll response -> token storage | 扫码确认后的 JWT token 进入本地存储 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | qr_poll response token | mitigate | qr_poll 返回 token 后，Plan 02 中通过 user_info() 验证 token 有效性再存储，与 try_token_probe 相同模式 |
| T-04-02 | Information Disclosure | loginuuid in headers | accept | loginuuid 是设备指纹，非敏感数据，与现有 login API 使用相同 header |
| T-04-03 | Denial of Service | 轮询频率 | mitigate | 固定 1s 间隔 + 60s 超时 + 3 次网络错误后暂停（Plan 02 UI 层实现） |
</threat_model>

<verification>
1. `python -m pytest tests/test_pan_api.py -x -v -k "qr"` — 所有 QR API 测试通过
2. `grep -n "qr_generate\|qr_poll" src/app/common/api.py` — 确认两个方法存在
3. `grep "qrcode" pyproject.toml` — 确认依赖已添加
</verification>

<success_criteria>
- Pan123.qr_generate() 可调用 login.123pan.com 生成二维码会话
- Pan123.qr_poll(uni_id) 可轮询扫码状态并返回 loginStatus + token
- pyproject.toml 包含 qrcode 和 Pillow 依赖
- 6+ 个单元测试全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/04-qr-login/04-01-SUMMARY.md`
</output>
